import { PreventSpaceDirective } from './prevent-space.directive';

describe('PreventSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
